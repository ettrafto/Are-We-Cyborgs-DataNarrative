import React from 'react';

const Compare = (()=>{
    return(
        <div>//prompt for hours of screentime yesterday + submit
        //generates bell graph of screentime with marker
        //total number of years on phone in life
        </div>
    );
});

export default Compare;